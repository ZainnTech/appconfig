db:
  url: jdbc:postgresql://localhost:5432/user_db
  username: 'admin1234'
  password: 'admin1234'

spring:
  datasource:
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true


keystore-backend:
  classpath: certs/keystore-backend.jks
  password: Admin$123
  alias: mykey
keystore-client:
  classpath: certs/keystore-client.jks
  password: Client$123
  alias: mykey


keycloak:
  url: http://localhost:8070/
  realm: zain-soft-technology
  clientId: inventory-client-credentials
  clientSecret: Xl4fIwSDP5X7yT1TDH9Yw4XipOofrq7l
